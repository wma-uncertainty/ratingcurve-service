version: '3'
services:

  ratingcurve:
    image: rating-service
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: rating-service
    ports:
     - "4000:80"
    environment:
      - PORT=80
    restart: "no"
